/// @deprecated - Audio emitters are now spawned dynamically by sfx_diegetic_play
/// This function is kept for backwards compatibility but always returns undefined
function tile_audio_emitter(_x, _y)
{
    return undefined;
}
