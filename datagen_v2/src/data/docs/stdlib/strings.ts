import { Doc } from "../../../lib";

export const strings = new Doc("Strings and Types")
    .function("string", "val", "string", "Converts any value to its string representation.", [["`val`", "any", "Value to convert"]], 'string(123); // "123"')
    .function("real", "val", "number", "Converts a string to a number.", [["`val`", "string", "String containing a number"]], 'real("42"); // 42')
    .function("string_length", "str", "number", "Returns the length of a string.", [["`str`", "string", "Input string"]], 'string_length("Hello"); // 5')
    .function("string_pos", "substr, str", "number", "Finds the position of a substring within a string.", [["`substr`", "string", "Substring to find"], ["`str`", "string", "String to search in"]], 'string_pos("lo", "Hello"); // 4')
    .function("string_delete", "str, index, count", "string", "Removes a part of a string.", [["`str`", "string", "Input string"], ["`index`", "number", "Start position (1-indexed)"], ["`count`", "number", "Number of characters to delete"]], 'string_delete("Hello World", 6, 6); // "Hello"')
    .function("string_insert", "str, substr, index", "string", "Inserts a substring into a string.", [["`str`", "string", "Base string"], ["`substr`", "string", "String to insert"], ["`index`", "number", "Insertion position (1-indexed)"]], 'string_insert("World", "Hello ", 1); // "Hello World"')
    .function("string_replace", "str, old, new", "string", "Replaces the first occurrence of a substring.", [["`str`", "string", "Original string"], ["`old`", "string", "Substring to replace"], ["`new`", "string", "Replacement text"]], 'string_replace("Hello World", "World", "Daydream"); // "Hello Daydream"')
    .function("string_replace_all", "str, old, new", "string", "Replaces all occurrences of a substring.", [["`str`", "string", "Original string"], ["`old`", "string", "Substring to replace"], ["`new`", "string", "Replacement text"]], 'string_replace_all("ho ho ho", "ho", "he"); // "he he he"')
    .function("string_upper", "str", "string", "Converts a string to uppercase.", [["`str`", "string", "Input string"]], 'string_upper("hello"); // "HELLO"')
    .function("string_lower", "str", "string", "Converts a string to lowercase.", [["`str`", "string", "Input string"]], 'string_lower("HELLO"); // "hello"')
    .function("string_width", "str", "number", "Returns the width of the string in pixels based on current font.", [["`str`", "string", "Input string"]])
    .function("string_height", "str", "number", "Returns the height of the string in pixels based on current font.", [["`str`", "string", "Input string"]])
    .function("chr", "code", "string", "Converts an ASCII/Unicode value to a character.", [["`code`", "number", "Character code"]], "chr(65); // \"A\"")
    .function("ord", "char", "number", "Converts a character to its ASCII/Unicode value.", [["`char`", "string", "Single character"]], 'ord("A"); // 65')
    .function("is_string", "val", "boolean", "Returns true if the value is a string.", [["`val`", "any", "Value to check"]])
    .function("is_real", "val", "boolean", "Returns true if the value is a number.", [["`val`", "any", "Value to check"]])
    .function("is_numeric", "val", "boolean", "Returns true if the value is numeric (real or int64).", [["`val`", "any", "Value to check"]])
    .function("is_bool", "val", "boolean", "Returns true if the value is a boolean.", [["`val`", "any", "Value to check"]])
    .function("is_array", "val", "boolean", "Returns true if the value is an array.", [["`val`", "any", "Value to check"]])
    .function("is_struct", "val", "boolean", "Returns true if the value is a struct.", [["`val`", "any", "Value to check"]])
    .function("is_undefined", "val", "boolean", "Returns true if the value is undefined.", [["`val`", "any", "Value to check"]])
    .function("is_regex", "val", "boolean", "Returns true if the value is a regex object.", [["`val`", "any", "Value to check"]])
    .function("typeof", "val", "string", "Returns a string describing the type of the value.", [["`val`", "any", "Value to check"]], "typeof(123); // \"number\"")
    .toString();
