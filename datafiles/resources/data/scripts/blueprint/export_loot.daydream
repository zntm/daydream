var t = tile
var loot_id = t.get_component("loot_id")

if (loot_id == undefined || loot_id == "") {
    show_debug_message("Error: Loot ID is empty");
    return;
}

show_debug_message("Exporting loot table: " + loot_id);

// Perform export...

if (element != undefined && element.instance != undefined) {
    ui_destroy(element.instance);
}
